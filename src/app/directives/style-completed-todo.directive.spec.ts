import { StyleCompletedTodoDirective } from './style-completed-todo.directive';

describe('StyleCompletedTodoDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleCompletedTodoDirective();
    expect(directive).toBeTruthy();
  });
});
